type Book {
    id: String
    title: String
    author: String
}

type Query {
    books: [Book]
    book(id: String): Book
}

type Mutation {
    addBook(title: String!, id: ID!, author: String!): Book
}
